/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[{
            hostname: 'i.imgur.com',
            pathname: '/mepIbN1.png'
        }]
    }
}

export default nextConfig;